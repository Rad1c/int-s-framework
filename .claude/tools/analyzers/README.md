# Analyzers

Analyzers inspect code, configuration, artifacts, or repository structure and report findings without owning the inspected system.

Each analyzer must follow the tool contract documented in `tools/README.md`.
