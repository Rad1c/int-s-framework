# Workspace

Temporary work. Nothing here is registered, documented, or expected to survive.

`generated/` holds one-off automation. The second time you need a script that lives here, propose
promoting it to `.claude/tools/candidate/` — see `.claude/tools/README.md` for the lifecycle.
